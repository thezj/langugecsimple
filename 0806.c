#include <stdio.h>

#define length 10
#define width 5
const int height = 10;
#define newline "\n"

void main()
{
    int cube;

    cube = length * width * height;
    printf("value of area : %d", cube);
    printf("%s", newline);

    while (1);
}
